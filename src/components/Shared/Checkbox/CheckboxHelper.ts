import { ICommonHelperParams } from '@base/BaseInterface'
import React from 'react'
import { ICheckboxProps, ICheckboxState } from './CheckboxType'

export const useCheckboxHelper = (params:ICommonHelperParams<ICheckboxProps,ICheckboxState>) => {
  return {}
}
